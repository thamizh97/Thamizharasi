#include<stdio.h>
void main()
{
  int hours, minutes, totalminutes;
  printf("enter the total minutes");
  scanf("%d",&totalminutes);
  {
    totalminutes=(hours*60)+minutes;
  }
  printf(" the hours  %d are",hours,minutes);
}
