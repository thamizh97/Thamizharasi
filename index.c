#include<stdio.h>
void main()
{
int i,a[i],n;
printf("enter the values");
scanf("%d",&n);
printf("%d \n",&i);
printf("the elements %d are: ");
for(i=0;i<n;i++)
{
printf("%d%d",i,a[i]);
scanf("%d%d",i,a[i]);
}
}
